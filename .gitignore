target/
node_modules/
.anchor/
test-ledger/
.env
.env.*
!.env.example
.DS_Store
*.log
apps/web/.next/
apps/web/tsconfig.tsbuildinfo
scripts/.session-*.json
scripts/migrations.json
apps/web/public/migrations.json

# entry-gate co-signer — server-only secret
gate.json

# mainnet canary run artifacts — RPC keys inline, local only
scripts/mainnet-canary/
.grok-artifacts/

# local dev-server config for the preview pane
.claude/

# pump.fun mainnet fixtures for litesvm (10 MB ELF) — captured locally by scripts/dump-pump-fixtures.mjs
litesvm-tests/fixtures/
# pump.fun mint keypairs — the CA is known before launch, the secret never leaves this machine
scripts/pump-mints/

# pump canary: three throwaway trader keypairs + run state, local only
scripts/pump-canary/
