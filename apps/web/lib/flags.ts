/* Switches for things that are built, styled and working but not wanted on
 * the page right now. Nothing here is deleted — flip a flag and the feature
 * comes back exactly as it was. */

/** The "Nothing to snipe." pitch block above the board. */
export const SHOW_PITCH = false;

/** The live-market state chip (DARK / BONDING). The settled chips
 *  (RECONCILED, GRADUATED) are unaffected — those say something. */
export const SHOW_DARK_CHIP = false;
